@model TransactionsData.Models.ProviderModel
@{
    ViewData["Title"] = "New Provider";
}
<h1>Provider</h1>
<div class="container jumbotron">
    @using (Html.BeginForm("CreateProvider", "Product", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, true, new { @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <div class="col-md-12">
            <div class="form-group row control-label">
                <div class="col">
                    Provider Name:
                    @Html.EditorFor(model => model.providerName, new { htmlAttributes = new { @class = "form-control", name = "mid" } })
                </div>
                <div class="col">
                    Provider Code:
                    @Html.EditorFor(model => model.providerCode, new { htmlAttributes = new { @class = "form-control" } })
                </div>
                <div class="col">
                    Status:
                    @{
                        var statues = new List<SelectListItem>()
                                                                                                                                                                                                                                                                                                                                            {
                            new SelectListItem() { Text = "Enabled", Value = "Enabled" },
                            new SelectListItem() { Text = "Disabled", Value = "Disabled" },
                        };
                    }
                    @Html.DropDownListFor(model => model.status, statues, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group row control-label">
                <div class="col float-left col-md-8">
                    <input type="submit" value="Create Provider" class="btn btn-primary"/>                    
                </div>
            </div>
        </div>
    }
</div>
